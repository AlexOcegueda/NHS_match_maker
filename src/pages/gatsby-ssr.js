// gatsby-ssr.js

// This file is executed during server-side rendering (SSR) and allows you to
// customize the behavior of your Gatsby site on the server.

// Example: Import global CSS styles
import '../styles/global.css';

// You can use Gatsby's Server-side Rendering APIs to customize your site's behavior.
// For example:
// exports.onPreRenderHTML = ({ getHeadComponents, replaceHeadComponents }) => {
//   // Do something before the HTML is rendered.
//   const headComponents = getHeadComponents();
//   // Modify headComponents array if needed
//   replaceHeadComponents(headComponents);
// };
